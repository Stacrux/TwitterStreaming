SELECT mentionedUser, count(*)
FROM TwitterBean.win:time(60)
group by mentionedUser
having count(*) > all(
	select count(*)
	from TwitterBean.win:time(60)
	group by mentionedUser
)
output snapshot every 30 seconds