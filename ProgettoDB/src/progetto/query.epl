SELECT mentionedUser, count(*), user
FROM TwitterBean.win:time(60 seconds)
where lang = 'en'
GROUP BY mentionedUser
HAVING count(*) >= ALL(
	select count(*)
	from TwitterBean.win:time(60 seconds)
	group by mentionedUser
	)
output every 30 seconds